<analysis>
The previous AI engineer successfully transformed Emergent into JMMC Invoicing, implementing crucial features like AI content verification, row-moving, and an invoice status lifecycle. The primary focus of this trajectory was to enable real e-računi API integration. Initial efforts debugged a stub mode configuration, identified and corrected an incorrect Slovenian API endpoint, and resolved OCR errors in API credentials. Post-integration success, the engineer addressed a user request to automatically populate and make non-editable the invoice number from e-računi after posting. Backend logic was updated for this, but comprehensive frontend verification of the new invoice number display on a freshly posted invoice was incomplete due to navigation issues. The drag to reorder feature remains an explicit pending task.
</analysis>

<product_requirements>
The goal is to develop an MVP for JMMC Invoicing for monthly invoicing using FastAPI, MongoDB, and React. Core features include:
1.  **Auth & Seed:** Username/password with JWT, Argon2, initial admin/user accounts (now with static passwords).
2.  **XLSX Import:** Drag & drop XLSX files, validation, date inputs.
3.  **Three Phases:** IMPORT (), EDIT (compose invoices, edit lines, calculate amounts), REVIEW & POST (totals, warnings, post to ).
4.  **eRačuni Connector:** Integration with , credentials, and API endpoint.
5.  **MongoDB Collections:** , , , , , , , .
6.  **API (FastAPI):** , ,  (compose, get, put, post), .
7.  **UI Pages (React):** , , , , , , .
8.  **Security:** Argon2, JWT guards, ADMIN role.
9.  **Implemented features (current state):** Seeded users (fixed passwords), functional login, comprehensive XLSX import, batch management (list, detail, edit metadata, archive status), AI Agent settings with editable prompts, AI-powered content verification,  migration,  API configuration and posting, move row functionality, enhanced invoice status lifecycle, and extensive UI/UX improvements. The  API now successfully posts invoices and the invoice  field is populated from the API response and is read-only.
</product_requirements>

<key_technical_concepts>
-   **FastAPI:** Python backend for API development.
-   **React:** Frontend library for UIs.
-   **MongoDB:** NoSQL database, with Motor.
-   **Pydantic:** Data validation.
-   **JWT & Argon2:** Authentication.
-   **Shadcn UI & Sonner:** UI components and notifications.
-   **httpx:** Asynchronous HTTP client.
-   **XLSX Parsing:** Excel file processing.
</key_technical_concepts>

<code_architecture>
The application uses a standard full-stack structure with a FastAPI backend and a React frontend.



-   **/app/backend/server.py**:
    -   **Importance:** Core FastAPI application handling backend logic, API endpoints, and database interactions.
    -   **Changes Made:**
        -   Existing  endpoint was previously enhanced for real e-računi API integration (removed stub, added  payload, error handling).
        -   **New Change:** Modified to set the  field of an invoice with  received from  after a successful post.
        -   **New Change:** Updated stub mode logic to also set a generated  field.
        -   Existing  endpoint was previously updated to mark as  on changes and handle editable date fields.
        -   **New Change:** Modified to prevent the  field from being updated if the invoice  is already posted, ensuring the e-računi number is authoritative.
        -    model updated to include , , , , .
        -   New endpoint:  for e-računi API connection testing.
-   **/app/backend/.env**:
    -   **Importance:** Stores environment variables for backend configuration.
    -   **Changes Made:**  was changed from  to  to enable actual e-računi API calls.
-   **/app/frontend/src/pages/InvoiceDetail.js**:
    -   **Importance:** Handles display and interaction for individual invoices.
    -   **Changes Made:**
        -    function was updated to display detailed API request/response errors.
        -   **New Change:** The Invoice Number input field was made read-only and a placeholder Auto-assigned after posting was added when the number is not yet set.
        -   **New Change:** Removed the Invoice number not set warning previously shown for unposted invoices.
        -   Action buttons and editable fields disabled/read-only when invoice status is posted.
-   **/app/frontend/src/pages/Settings.js**:
    -   **Importance:** Manages application settings, including AI and e-računi configurations.
    -   **Changes Made:** Redesigned with tile-based layout.  section added with credentials, endpoint fields, and Test Connection button. The default API Endpoint was updated to .
</code_architecture>

<pending_tasks>
-   Implement drag to reorder functionality for invoice line items in the Edit phase.
-   Verify the frontend display of the automatically populated and read-only invoice number after posting a new invoice to e-računi.
</pending_tasks>

<current_work>
Immediately before this summary, the AI engineer was addressing the user's request to fix how the invoice number is handled after posting to e-računi. The user wanted the invoice number to be automatically set from the e-računi API response and made non-editable, also removing a Invoice number not set warning.

The following changes were implemented:
1.  **Backend ():**
    *   The  endpoint was modified to extract the  (now confirmed to be the actual invoice number from e-računi) and store it in the invoice's  field in MongoDB.
    *   The  endpoint was updated to prevent modifications to the  field if the invoice's status is already posted, ensuring the e-računi assigned number is preserved.
    *   The stub mode for  was also updated to generate and set a  field to mimic the real API.
2.  **Frontend ():**
    *   The invoice number input field was made read-only.
    *   A placeholder text, Auto-assigned after posting, was added to guide the user.
    *   The warning message Invoice number not set for unposted invoices was removed.

At the very end of the trajectory, the AI engineer confirmed that the backend logic for setting the  field from e-računi response is correct for *newly posted invoices* by checking the database. However, due to ongoing frontend navigation and rendering issues (blank pages, incorrect page displays during screenshot attempts), the actual visual verification of these changes on a freshly posted invoice in the  page was not completed.
</current_work>

<optional_next_step>
Post a new draft invoice to e-računi via the UI and verify that its  field is auto-populated and read-only on the Invoice Detail page.
</optional_next_step>

